<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration PUBLIC
        "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd" >

<configuration>
  <!-- 为每个model类配置别名 -->
  <typeAliases>
    <typeAlias alias="User" type="com.netease.model.User"/>
    <typeAlias alias="Article" type="com.netease.model.Article"/>
  </typeAliases>

  <plugins>
    <plugin interceptor="com.netease.util.PagePlugin">
      <property name="dialect" value="mysql" />
      <property name="pageSqlId" value=".*ListPage.*" />
    </plugin>
  </plugins>

  <!-- 手动加载model的映射xml文件 -->
  <!--<mappers>-->
    <!--<mapper resource="mapper/User.xml"/>-->
  <!--</mappers>-->
</configuration>
